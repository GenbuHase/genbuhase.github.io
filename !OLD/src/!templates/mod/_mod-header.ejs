<%
	include("../_global");

	init({
		name: '""',
		hasIcon: 'false'
	});
%><%-
	include("../_header", {
		name,
		title: `${name}｜Mod`,
		favicon: hasIcon ? "./favicon.ico" : null,
		
		libraries: [
			{ type: "STYLE", src: "../Detail.css" }
		]
	}) %>
			<%- include("./_modname", { name, hasIcon }) %>
			