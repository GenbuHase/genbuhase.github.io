<%
	include("./_global");

	init({
		name: '""',
		logs: '[]'
	});
%><Table Class = "highlight">
	<THead>
		<Tr><Th>Version (Released date)</Th><Th>Contents</Th></Tr>
	</THead>

	<TBody><%
for (const log of logs) {
	if (needInit(log.changes)) log.changes = []; %>
		<Tr>
			<Td><%= `${name} ${log.version} (${log.releasedAt})` %></Td>

			<Td>
				<% for (const change of log.changes) { %><%= change %><Br />
				<% } %>
			</Td>
		</Tr><% } %>
	</TBody>
</Table>