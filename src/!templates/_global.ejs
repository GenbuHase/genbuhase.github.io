<%
	needInit = (variable) => (typeof variable === "undefined");

	init = (defaultConfigs = {}) => {
		for (const config in defaultConfigs) {
			try {
				eval(config);
			} catch (error) {
				eval(`${config} = ${defaultConfigs[config]}`);
			}
		}
	};

	SITENAME = "どっかのプログラなーいのサイト。";
%>